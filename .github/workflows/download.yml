name: Remote Download to OneDrive

on:
  workflow_dispatch:
    inputs:
      url:
        description: "Download URL (https://...)"
        required: true
      filename:
        description: "Save as (optional, leave blank to auto-detect)"
        required: false
        default: ""
      folder:
        description: "OneDrive folder (leave blank for root)"
        required: false
        default: ""
  schedule:
    - cron: "0 0 1 * *"

permissions:
  contents: read

jobs:
  download:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    env:
      RCLONE_CONF_DATA: ${{ secrets.RCLONE_CONF }}
    steps:
      - name: Detect mode
        id: mode
        run: |
          if [ "${{ github.event_name }}" = "schedule" ]; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "Scheduled keep-alive run"
          else
            echo "skip=false" >> $GITHUB_OUTPUT
          fi

      - name: Mask sensitive inputs
        if: steps.mode.outputs.skip == 'false'
        env:
          URL: ${{ inputs.url }}
          FILENAME: ${{ inputs.filename }}
        run: |
          echo "::add-mask::$URL"
          [ -n "$FILENAME" ] && echo "::add-mask::$FILENAME" || true

      - name: Setup rclone
        run: |
          curl -sLo /tmp/rclone.zip https://downloads.rclone.org/rclone-current-linux-amd64.zip
          unzip -q /tmp/rclone.zip -d /tmp
          sudo mv /tmp/rclone-*-linux-amd64/rclone /usr/local/bin/
          mkdir -p ~/.config/rclone
          printf '%s\n' "$RCLONE_CONF_DATA" > ~/.config/rclone/rclone.conf
          rclone version

      - name: Refresh token (ping OneDrive)
        run: |
          rclone lsd onedrive: --max-depth 1 > /dev/null
          echo "Token verified."

      - name: Download and stream to OneDrive
        if: steps.mode.outputs.skip == 'false'
        env:
          URL: ${{ inputs.url }}
          FILENAME: ${{ inputs.filename }}
          FOLDER: ${{ inputs.folder }}
        run: |
          set -e
          if [ -z "$FILENAME" ]; then
            FILENAME=$(basename "${URL%%\?*}")
          fi
          if [ -z "$FILENAME" ] || [ "$FILENAME" = "/" ]; then
            FILENAME="download_$(date +%s)"
          fi
          echo "::add-mask::$FILENAME"
          if [ -z "$FOLDER" ]; then
            DEST="onedrive:$FILENAME"
          else
            DEST="onedrive:$FOLDER/$FILENAME"
          fi
          echo "Streaming to OneDrive..."
          curl -sSL \
            -A "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36" \
            --retry 3 --retry-delay 5 \
            "$URL" | rclone rcat "$DEST" \
            --onedrive-chunk-size 100M \
            --stats=30s \
            --stats-one-line
          echo "Done."

      - name: Update refreshed token in repo secret
        env:
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          NEW_CONF=$(cat ~/.config/rclone/rclone.conf)
          if [ "$NEW_CONF" != "$RCLONE_CONF_DATA" ]; then
            echo "Token changed, updating secret..."
            gh secret set RCLONE_CONF --body "$NEW_CONF" --repo ${{ github.repository }}
            echo "Secret updated."
          else
            echo "No token change, secret stays."
          fi
